To run app, make sure to add `.env` with 
```
PORT={port}
DATABASE_URL=mysql://{username}:{password}@{db_url}/{db_name}
```
To run client, make sure to add `.env` with `REACT_APP_API_BASE_URL=http://localhost:5000`